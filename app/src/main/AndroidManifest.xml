<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.management_system">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Management_system"
        android:usesCleartextTraffic="true"
        tools:targetApi="n">
        <activity android:name=".module.account.LoginOrRegisterActivity">

        </activity>
        <activity android:name=".module.main.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".module.mine.SettingActivity" />
        <activity android:name=".module.mine.PasswordChangeActivity" />
        <activity android:name=".module.pay.PaymentItemActivity" />
        <activity android:name=".module.owner.OwnerDetailActivity" />
        <activity android:name=".module.house.HousingStatisticsActivity" />
        <activity android:name=".module.owner.SearchActivity"/>
    </application>

</manifest>